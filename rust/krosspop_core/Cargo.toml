[package]
name = "krosspop_core"
version = "0.1.0"
edition = "2021"

[lib]
# Linked into the C++ firmware via a C ABI boundary — see ROADMAP.md.
crate-type = ["staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
